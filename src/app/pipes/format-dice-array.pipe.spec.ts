import { FormatDiceArrayPipe } from './format-dice-array.pipe';

describe('FormatDiceArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDiceArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
